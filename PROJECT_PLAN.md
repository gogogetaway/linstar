# 网站部署、响应式、多语言与语音规划

## 1. 当前项目判断

当前项目目录是 `www.blxing.com/`，主要由静态文件组成：

- 页面：约 66 个 `.html` 页面，另有大量 `.orig` 备份文件。
- 静态资源：`css/`、`js/`、`img/`、`upload/`、`jquery-1.4.4.min.js`。
- 没有发现 `package.json`、构建脚本、后端代码、数据库配置或 Docker 配置。
- 站点可以按静态网站方式部署，不需要 Node、PHP 或数据库才能访问现有页面。

结论：第一阶段应先把它整理成可直接部署的静态网站，确保服务器上 `index.html` 可访问，资源路径正确，常见页面不 404。

## 2. 总目标

1. 部署到服务器后可以直接使用。
2. 同时兼容电脑端和手机端。
3. 支持多语言内容：英文、中文、波斯语、俄语。
4. 支持语音能力：至少包含页面文字朗读；后续可扩展为语音选择、语音介绍、语音搜索。

## 3. 第一阶段：服务器直接部署可用

目标：不改大结构，先让现有网站稳定上线。

### 需要做的事

- 保留 `www.blxing.com/` 作为网站根目录，服务器根目录直接指向该文件夹。
- 确认 `index.html` 是默认首页。
- 清理或排除 `.orig` 文件，避免被服务器公开访问。
- 检查所有页面里的资源路径，确保 `css/`、`js/`、`img/`、`upload/` 都能正常加载。
- 增加基础部署文件：
  - `nginx.conf.example`：Nginx 静态站点配置示例。
  - `.htaccess`：如果用 Apache，可以禁止访问 `.orig` 文件。
  - `DEPLOY.md`：写清楚上传、服务器配置、验证步骤。
- 检查外部依赖：
  - Google Fonts 在 `css/c.css` 中远程加载，部分地区可能慢或失败，建议后续本地化字体。
  - 联系页使用高德地图脚本，但 key 还是占位值，需要正式 key 或替换方案。
  - 关于页使用 Wistia 视频脚本，依赖外网可访问性。

### 推荐服务器方案

- 推荐：Nginx + 静态文件部署。
- 目录示例：`/var/www/blxing/www.blxing.com`
- 域名根目录指向：`/var/www/blxing/www.blxing.com`
- 默认首页：`index.html index.php.html`
- 禁止访问：`*.orig`

### 第一阶段验收标准

- 打开域名可以进入首页。
- 首页、产品页、产品详情页、关于我们、新闻、联系页都能访问。
- 图片、CSS、JS 正常加载。
- 手机和电脑都能打开，不出现明显资源 404。
- `.orig` 文件不能通过浏览器访问。

## 4. 第二阶段：电脑端和手机端兼容

目标：保留现有视觉结构，修复移动端溢出、错位、图片拉伸和表格撑宽问题。

### 当前风险

- 页面里大量使用内联 CSS、固定宽度、`float` 布局和重复样式。
- 产品详情里有大表格、长英文、`nowrap` 内容，手机端容易横向滚动。
- 多数页面内嵌重复导航、菜单、底部和表单样式，后续维护成本高。
- `jquery-1.4.4` 很老，短期可以保留，长期建议替换或移除。

### 改造策略

- 先新增一个全局兼容样式文件，例如 `css/responsive-fix.css`，统一处理移动端问题。
- 批量给所有 HTML 引入该 CSS，而不是逐页手改内联样式。
- 增加通用规则：
  - `img { max-width: 100%; height: auto; }`
  - 表格在手机端允许横向滚动或改成块状显示。
  - 容器最大宽度使用 `max-width`，避免固定宽度撑破屏幕。
  - 产品列表在手机端改为单列，平板端双列，电脑端多列。
  - 导航菜单、搜索框、底部联系方式适配小屏。
- 对核心页面逐个验证：
  - 首页
  - 产品列表页
  - 产品详情页
  - 关于页
  - 联系页

### 第二阶段验收标准

- iPhone 宽度 375px 不出现整体横向滚动。
- Android 常见宽度 360px/393px 页面可读。
- iPad/平板布局不塌陷。
- 桌面端 1366px/1440px/1920px 展示正常。
- 产品详情表格和图片在手机端可读可操作。

## 5. 第三阶段：多语言规划

目标：支持英文、中文、波斯语、俄语，并能长期维护。

### 语言代码

- 英文：`en`
- 中文：`zh-CN`
- 波斯语：`fa`
- 俄语：`ru`

说明：用户说的“伊朗语音”实际更准确应为波斯语/法尔西语，语言代码通常使用 `fa`。波斯语是从右到左书写，需要单独处理 `dir="rtl"`。

### 推荐实现方式

现有项目是静态页面，推荐先做“静态多语言目录”：

- `/en/`：英文版本
- `/zh-CN/`：中文版本
- `/fa/`：波斯语版本
- `/ru/`：俄语版本

每个目录保留对应语言的 HTML 页面。这样部署简单、SEO 友好、服务器要求低。

### 不推荐的方式

- 不建议只用浏览器自动翻译，因为内容质量不可控，SEO 不稳定。
- 不建议一开始就上复杂后台或数据库，当前项目没有后端基础，会显著增加开发量。

### 内容处理流程

1. 先确定英文版作为源内容。
2. 提取导航、底部、按钮、表单、产品分类、产品标题、详情正文。
3. 建立翻译表或内容清单，例如 `i18n/content-map.md`。
4. 分批翻译：
   - 第一批：导航、首页、关于、联系、产品分类。
   - 第二批：产品列表和产品详情。
   - 第三批：新闻和辅助页面。
5. 波斯语页面设置：
   - `<html lang="fa" dir="rtl">`
   - 字体使用支持阿拉伯/波斯字符的字体。
   - 导航和产品内容做 RTL 方向测试。
6. 中文、俄语页面设置对应 `lang` 属性。

### 语言切换

- 页面顶部增加语言切换入口：`EN / 中文 / فارسی / Русский`
- 同一页面不同语言之间互相跳转。
- 如果某语言页面暂未翻译完成，先跳转到该语言首页或显示“内容整理中”。

### 第三阶段验收标准

- 四种语言首页可访问。
- 导航、底部、联系信息、产品分类完成翻译。
- 每个语言页面有正确 `lang`。
- 波斯语页面方向正确，不出现左右错乱。
- 语言切换不会跳到 404。

## 6. 第四阶段：语音能力规划

目标：先实现低成本可用的文字朗读，再根据需要扩展更自然的真人/AI 语音。

### 方案 A：浏览器原生朗读，推荐先做

使用 Web Speech API，让浏览器朗读页面文字。

优点：

- 不需要服务器。
- 不需要 API 费用。
- 可以快速支持英文、中文、波斯语、俄语。
- 适合第一版上线。

缺点：

- 不同浏览器和手机系统支持的声音不完全一致。
- 波斯语声音在部分设备上可能缺失。
- 音质取决于用户设备。

功能设计：

- 增加“朗读本页”按钮。
- 增加“暂停/继续/停止”按钮。
- 根据当前页面语言自动选择朗读语言：
  - 英文：`en-US` 或 `en-GB`
  - 中文：`zh-CN`
  - 波斯语：`fa-IR`
  - 俄语：`ru-RU`
- 只朗读主要内容区域，避免把导航、底部、菜单全部读出来。

### 方案 B：预生成音频，适合产品介绍

为重要页面或产品介绍生成 `.mp3` 音频并放到服务器。

优点：

- 音质稳定。
- 所有浏览器都容易播放。
- 可以选择更自然的声音。

缺点：

- 每次内容修改都要重新生成音频。
- 多语言、多页面会产生较多音频文件。

适用范围：

- 首页公司介绍。
- 关于我们。
- 热门产品详情。
- 联系方式或采购说明。

### 方案 C：AI TTS 接口，适合后期

通过服务端调用 TTS 接口实时生成语音或缓存音频。

优点：

- 声音更自然。
- 可选不同性别、口音、风格。
- 可扩展语音客服或语音搜索。

缺点：

- 需要后端服务。
- 有接口费用。
- 需要处理缓存、并发和密钥安全。

### 第四阶段验收标准

- 每个语言首页可以朗读主要内容。
- 英文、中文、俄语能正常选择对应语音。
- 波斯语如果设备无声音，需要给出降级提示。
- 手机端点击播放符合浏览器限制，不自动强制播放。

## 7. 建议执行顺序

### 第 1 步：部署基础整理

- 新增部署文档和服务器配置示例。
- 屏蔽 `.orig`。
- 本地启动静态服务器验证页面路径。
- 修复明显 404 和错误资源。

### 第 2 步：移动端兼容修复

- 新增 `responsive-fix.css`。
- 批量引入到所有 HTML。
- 优先修复首页、产品列表、产品详情、联系页。

### 第 3 步：结构抽取和可维护性提升

- 统一公共导航、底部、浮动 WhatsApp、搜索框逻辑。
- 如果继续保持纯静态，可用简单脚本批量生成页面。
- 如果后期要频繁更新产品，建议再考虑引入静态站点生成器或轻量后台。

### 第 4 步：多语言第一版

- 先做 `/zh-CN/`、`/fa/`、`/ru/` 首页和公共导航。
- 再做产品分类、核心产品详情。
- 最后补齐新闻和所有历史页面。

### 第 5 步：语音第一版

- 使用 Web Speech API 加入朗读按钮。
- 只朗读页面主内容。
- 后续再考虑高质量 TTS 音频。

## 8. 主要风险和决策点

- 当前页面数量多且重复，直接逐页手改容易出错，建议后续用脚本批量处理。
- `.orig` 文件应视为备份，不建议部署公开访问。
- 搜索功能目前看起来是前端跳转或旧站逻辑，需要确认是否真的可用；静态部署下不能做后端搜索，除非改成前端索引搜索。
- 联系页地图 key 是占位值，需要申请正式 key 或改用静态地图/嵌入地图。
- 多语言内容需要人工确认专业术语，尤其是产品规格、贸易术语和波斯语/俄语翻译。
- 如果要后台管理产品、询盘表单入库、邮件通知，就不再是纯静态部署，需要新增后端。

## 9. 下一步建议

下一步先执行“第 1 步：部署基础整理”。完成后这个项目可以先放到服务器运行，再继续做移动端兼容和多语言/语音。

建议下一轮具体任务：

1. 新增 `DEPLOY.md` 和 `nginx.conf.example`。
2. 增加服务器规则，禁止访问 `.orig`。
3. 本地启动静态服务检查首页和核心页面。
4. 记录需要修复的 404、外部依赖和页面问题。
