# JLD 独立站优化路线图

## 文档范围

本文档记录首页与 Products 产品模块的优化计划。

当前网站已经具备 B2B 木制板材出口独立站的基础结构，但在正式上线前，
还需要完善询盘转化、产品检索、多语言、SEO 和性能体验。

## P0：上线前必须修复

### 1. 增加真正可用的询盘表单

现状问题：
- 首页 CTA 跳转到 `#quote`，但目标位置目前是订阅表单，不是询价表单。
- `js/home-v2.js` 已经包含询盘邮件生成逻辑，但页面中没有对应的
  `data-inquiry-form` 表单。
- 目前 Email 按钮使用 `mailto:`，点击后会打开访客电脑默认的邮件客户端，
  例如 Outlook。

建议方案：
- 增加页面内 RFQ 询价表单。
- 接入后端接口或第三方表单服务。
- WhatsApp 继续作为辅助联系方式。

最低字段要求：
- 产品名称
- 产品用途
- 尺寸
- 厚度
- 数量
- 目的港
- 联系邮箱
- WhatsApp
- 包装、证书和目标市场备注

验收标准：
- 访客不需要打开本地邮件客户端即可提交询盘。
- 销售能够收到全部 RFQ 字段。
- 页面能够显示提交成功或提交失败状态。

### 2. 修复全球市场数据动画

现状问题：
- 首页计数脚本会对所有 `.stat-item strong` 元素执行 `parseInt()`。
- `FOB/CIF` 不是数字，滚动到该模块后会变成 `NaN+`。

验收标准：
- 只有数字类指标执行动画。
- `FOB/CIF` 等文本指标保持原样。

### 3. 实现 Products 搜索与分类筛选

现状问题：
- 搜索框会跳转到 `products.html?q=...`，但 Products 页面没有读取 `q`。
- 左侧分类筛选只会跳转锚点并改变高亮状态，不会真正筛选内容。
- `All` 目前跳转到第一个分类，无法恢复展示全部产品。

验收标准：
- 搜索能够匹配产品名称、分类和常用别名。
- 分类按钮能够筛选或明确聚焦对应产品组。
- `All` 能够恢复展示全部产品。
- 没有搜索结果时，显示清晰提示和询盘入口。

### 4. 修复多语言逻辑

现状问题：
- `js/home-v2.js` 的中文字典中混入了西班牙语字段。
- 选择西班牙语时，目前会回退为英文。
- Products 和支持页面显示了语言按钮，但正文翻译并不完整。
- 产品详情数据目前基本只有英文。

建议方案：
- 分离 `en`、`zh` 和 `es` 三套字典。
- 某个语言版本未完整翻译前，不展示该语言入口。
- SEO 层面使用独立多语言 URL 和 `hreflang`，不要只依赖浏览器端替换文本。

验收标准：
- 切换语言后，单个页面不会混用多种语言。
- 所有可见语言入口在首页、Products 页面和产品详情页均有完整内容。

### 5. 核实业务数据与认证文件

需要确认：
- `30+` 年行业经验
- `12` 条生产线
- `50+` 个出口市场
- 是否存在冲突的 `100+ countries` 文案
- ISO 9001
- FSC
- CE / EN 13986
- EPA TSCA VI
- KS
- EUDR 支持
- ISPM 15
- EPD

验收标准：
- 每项公开数据都有内部材料支持。
- 文案需要区分：企业持有证书、可提供相关文件、协助处理目标市场要求。
- 所有页面使用统一的数据口径。

## P1：提升询盘转化

### 首页

建议内容顺序：
1. 清晰的首屏价值主张和询盘 CTA
2. 三类核心产品
3. 买家关注点：规格匹配、OEM 包装、混合装柜、装柜照片
4. 工厂与 QC 证明
5. 已核实的证书
6. 出口市场
7. RFQ 询盘表单
8. 页脚

首屏标题建议：

```text
Film Faced Plywood and Wood Panels
for Global Construction Projects
```

首屏副标题建议：

```text
Custom core, glue, thickness and export packing.
Send your specification for a container quotation.
```

建议按钮：
- 主按钮：`Get a Quote`
- 次按钮：`View Products`
- 辅助入口：`WhatsApp Sales`

其他优化：
- 首页只保留 4 至 6 个关键生产步骤。
- 完整工厂流程跳转到 About 页面。
- Facebook 和 TikTok 没有真实账号时，先移除占位链接。
- 统一品牌展示：`JLD` 和 `JIALINDA`。
- 确认现有 `@blxing.com` 邮箱是否需要替换为 JLD 品牌域名。

### Products 产品列表

产品卡片建议展示：

```text
Film Faced Plywood
厚度：8-25 mm
标准尺寸：1220 x 2440 mm
芯材：Poplar / Birch / Combi
胶水：Phenolic / Melamine
[查看规格] [获取报价]
```

验收标准：
- 买家不打开详情页也能初步比较产品。
- 每张产品卡片都有直接询价入口。
- 所有卡片使用一致的规格字段。

### 产品详情页

建议增加：
- 每个产品 3 至 5 张真实图片
- 产品图、包装图和装柜图
- 可下载产品参数表
- 页面内嵌 RFQ 表单
- 相关产品推荐
- MOQ
- 交期
- 付款方式
- 装柜数量
- FAQ：芯材、胶水、表面、颜色和复用次数
- 证书下载或证书申请入口

验收标准：
- 买家能够根据详情页信息准备有效询盘。
- 页面明确区分常规规格和可定制规格。

## P2：SEO 与性能优化

### SEO

建议改进：
- 为核心产品使用独立静态 URL，例如：

```text
/products/film-faced-plywood.html
/products/pp-plastic-faced-plywood.html
/products/lvl.html
```

- 增加 `canonical` URL。
- 增加 `robots.txt`。
- 增加 `sitemap.xml`。
- 增加 Open Graph 元数据。
- 增加 `Organization` 结构化数据。
- 为单个产品详情页增加 `Product` 结构化数据。
- 多语言完成后，增加多语言 URL 和 `hreflang`。
- 增加访问统计，以及 RFQ 和 WhatsApp 转化追踪。

### 性能

建议改进：
- 将体积过大的图片转换为优化后的 WebP 或 AVIF。
- 页眉 Logo 使用 SVG 或更小的透明 WebP。
- 为手机端提供尺寸合适的响应式图片。
- 为图片增加 `width` 和 `height`。
- 为首屏之外的图片增加 `loading="lazy"`。
- 首屏主图保持立即加载，可考虑增加 `fetchpriority="high"`。
- 正式上线前，检查并清理不再使用的旧版 CSS 和首页概念稿。

验收标准：
- 手机端不会下载不必要的桌面大图。
- 首屏之外的图片延迟加载。
- 使用 Lighthouse 检查首屏加载速度和页面布局稳定性。

## 已知代码位置

- 首页 CTA 和页脚表单：`index.html`
- 产品列表、搜索和侧边栏筛选：`products.html`
- 首页公共交互逻辑：`js/home-v2.js`
- 产品详情渲染和规格数据：`js/product-data.js`
- 首页公共样式：`css/home-v2.css`
- Products 与详情页样式：`css/styles.css`

## 建议实施顺序

1. 修复 RFQ 提交、计数动画、搜索、筛选和多语言逻辑。
2. 完善产品卡片和详情页询盘体验。
3. 核实业务数据、证书、域名和品牌邮箱策略。
4. 优化图片，并完善 SEO 基础设施。
5. 增加证书下载、市场内容和访问统计。
